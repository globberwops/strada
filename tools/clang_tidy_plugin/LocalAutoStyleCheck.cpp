#include "LocalAutoStyleCheck.h"

namespace clang::tidy::strada {

void LocalAutoStyleCheck::registerMatchers(ast_matchers::MatchFinder* Finder) {
  // Boilerplate: empty for now (will be implemented in future tasks)
}

void LocalAutoStyleCheck::check(const ast_matchers::MatchFinder::MatchResult& Result) {
  // Boilerplate: empty for now (will be implemented in future tasks)
}

}  // namespace clang::tidy::strada
